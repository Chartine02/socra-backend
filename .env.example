# =========================================================================
# SOCRA — Environment Variables (example)
# Copy to .env.local and fill in. NEVER commit .env.local.
# =========================================================================

# ---- Database ----
DATABASE_URL=

# ---- Auth ----
JWT_SECRET=
JWT_EXPIRES_IN=7d

# ---- Frontend ----
FRONTEND_URL=http://localhost:5173

# ---- AI Service ----
AI_SERVICE_URL=http://localhost:8001
AI_SERVICE_API_KEY=

# ---- Supabase Storage ----
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=socra-documents

# ---- LLM Providers (free tiers) ----
GEMINI_API_KEY=                     # https://aistudio.google.com/apikey
GROQ_API_KEY=                       # https://console.groq.com/keys

# ---- Observability (optional in dev) ----
SENTRY_DSN=
POSTHOG_KEY=

# ---- Canvas Integration (personal access token approach) ----
CANVAS_TOKEN_ENCRYPTION_KEY=       # run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# ---- Worker (FastAPI) — required from Week 3 ----
WORKER_BASE_URL=http://localhost:8000
WORKER_INTERNAL_TOKEN=              # shared HMAC between web <-> worker
